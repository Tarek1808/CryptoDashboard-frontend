/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i-invdn-com.investing.com', 'i.kinja-img.com' ,'images.bfmtv.com', 'www.broker-forex.fr','cdn.zonebourse.com'],
  },
};

module.exports = nextConfig;
